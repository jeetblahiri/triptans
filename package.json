{
  "name": "triptans",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-vercel.mjs",
    "dev": "python3 -m http.server 3000 --directory site",
    "test": "node test_static_model.mjs"
  }
}
